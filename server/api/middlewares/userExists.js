// import jwt from 'express-jwt';
// import config from '../../config';

const userExists = req => {
  console.log("SQL query here");
  // query for their email using req.params.email
  // if exists, return true
  // else, return false and don't proceed w POSTING/getting data or whatevs
};

export default userExists;
