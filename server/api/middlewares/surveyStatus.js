const surveyStatus = req => {
  console.log("SQL query here");
  // return {"status": 1, "data": "2019,10,12"}
  // return {"status": 2} if haven't completed survey
  // return {"status": 3} if they already send anki data
};

export default surveyStatus;
